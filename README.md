# keingsw's Dotfiles

Personal terminal configuration with iTerm2, Starship, and Mise.

## Quick Install

```bash
git clone git@github.com:keingsw/dotfiles.git ~/dotfiles
cd ~/dotfiles
chmod +x install.sh
./install.sh
```

## Manual Setup

See full installation guide in [INSTALL.md](INSTALL.md)
