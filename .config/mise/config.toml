[tools]
node = "lts"
# Global versions
# node = "lts"
# python = "3.12"

[settings]
# Automatically install missing tools when entering directories
auto_install = true

# Legacy version file support (.node-version, .ruby-version, etc.)
legacy_version_file = true

# Verbose output
verbose = false
